



















































────────────────────────────────────────────────────────────────────────────────




















































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>






































────────────────────────────────────────────────────────────────────────────────
>

















────────────────────────────────────────────────────────────────────────────────
>
>
















────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
















────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
>















────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch















────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch















────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>













────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>













────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
>












────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...












────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
>











────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux











────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>









────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>









────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
>








────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...








────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
>







────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64







────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>





────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>





────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
>




────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...




────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
>



────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern ''



────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern ''



────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern '{{}}'



────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern '{{}}'



────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern '{{.OS}}'



────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern '{{.OS}}'



────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern '{{.OS}} running on '



────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern '{{.OS}} running on {{}}'



────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern '{{.OS}} running on {{}}'



────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern '{{.OS}} running on {{.Arch}}'



────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern '{{.OS}} running on {{.Arch}}'
Linux running on AArch64
>

────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern '{{.OS}} running on {{.Arch}}'
Linux running on AArch64
>

────────────────────────────────────────────────────────────────────────────────
>
> # Get the *current* OS and CPU architecture...
> download-asset os-arch
linux/arm64
>
> # When the current OS matches 'linux', return a different value...
> download-asset os-arch --linux Linux
Linux/arm64
>
> # When the current CPU architecture matches 'arm64', return a different value...
> download-asset os-arch --linux Linux --arm64 AArch64
Linux/AArch64
>
> # Instead of {os}/{arch}, use a different pattern for matches...
> download-asset os-arch --linux Linux --arm64 AArch64 --pattern '{{.OS}} running on {{.Arch}}'
Linux running on AArch64
>
logout
────────────────────────────────────────────────────────────────────────────────
